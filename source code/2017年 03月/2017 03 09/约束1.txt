0、设置cjb中的学号和课程号为外键。
1、创建XSB1表（假设XSB1表未创建），并对“学号”字段创建PRIMARY KEY约束，对“姓名”字段定义UNIQUE约束。

2、创建一个course_name表来记录每门课程的学生学号、姓名、课程号、学分和毕业日期。其中，学号、课程号和毕业日期构成复合主键，学分为唯一键。

3、修改XSB1表，向其中添加一个“身份证号码”字段，对该字段定义UNIQUE约束。对“出生时间”字段定义UNIQUE约束。

4、删除上例中创建的PRIMARY KEY约束和UNIQUE约束。

